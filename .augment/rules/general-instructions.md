# Slash commands

When you see a message starting with `/`, it is most probably a custom command. Find it in
`.augment/commands` (either in `~`, or in project root) and follow the instructions there (plus any
additional instructions after the command).
